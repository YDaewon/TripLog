<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.trip.board.model.mapper.BoardMapper">

	<resultMap type="BoardDto" id="board">
		<result column="article_no" property="articleNo"/>
		<result column="title" property="title"/>
		<result column="content" property="content"/>
		<result column="created_at" property="registerTime"/>
		<result column="notice" property="notice"/>
		<result column="user_no" property="userNo"/>
	</resultMap>

	<insert id="createBoard" parameterType="BoardDto">
		insert into article (user_no, title, content) 
		values (#{userNo}, #{title}, #{content})
	</insert>

	<select id="listNotice" resultMap="board">
		select article_no, title, content, created_at, notice, user_no
		from article
		where notice = 1
		order by article_no
	</select>
	
	<select id="listAll" resultMap="board">
		select article_no, title, content, created_at, notice, user_no
		from article
		order by article_no
	</select>
	
	<select id="getBoard" parameterType="int" resultMap="board">
		select article_no, user_no, title, content, notice, created_at
		from article
		where article_no = #{articleNo}
	</select>
	
	<update id="modifyBoard" parameterType="BoardDto">
		update article set
			title = #{title},
			content = #{content}
		where article_no = #{articleNo}
	</update>
	
	<delete id="deleteBoard" parameterType="int">
		delete from article
		where article_no = #{articleNo}
	</delete>
	
	<!-- ADMIN -->
	<insert id="createNotice" parameterType="BoardDto">
		insert into article (user_no, title, content, notice) 
		values (#{userNo}, #{title}, #{content}, #{notice})
	</insert>
	
</mapper>